# 보안 기록

## 과거 API 키 노출 (2026-08 조치 완료)

이 저장소의 초기 커밋 일부에 Google Gemini API 키가 평문으로 포함되어 있었다. 해당 커밋은 저장소가 공개 상태이던 기간에 존재했으므로, 그 키들은 **노출된 것으로 간주하고 폐기했다**.

| 항목 | 내용 |
|---|---|
| 영향받은 커밋 | `401f4fa`, `5b24e4a`, `75d3d90` |
| 조치 | 해당 키 전부 무효화(2026-08) |
| 검증 | 4개 키 전부 `generativelanguage.googleapis.com`에 직접 호출 → 4/4 HTTP 403 |
| 무효화 주체 | Google. 응답 메시지가 "Your API key was reported as leaked" |
| 현재 HEAD | 키 없음 — 코드에 남아 있지 않다 |
| 현재 코드의 Gemini 사용 | 없음. LLM 호출은 `OPENAI_API_KEY` 하나만 사용한다 |

### 무효화가 확인된 방식, 그리고 확인되지 않은 것

키가 이력에 남아 있다는 점을 역이용해, 노출된 4개를 이력에서 추출해 실제로 API에 호출했다. 4개 전부 403이 돌아왔고 응답 메시지는 다음과 같았다.

> Your API key was reported as leaked. Please use another API key.

**따라서 이 키들은 사용 불가능하다 — 이건 계정 접근 없이 외부에서 검증된 사실이다.**

동시에 이 증거가 말해주지 *않는* 것도 분명히 해둔다. 403의 주체는 Google이다. Google의 자동 스캐너가 공개 저장소에서 키를 발견하고 차단한 것이지, 소유자가 삭제했다는 증거가 아니다. 삭제된 키였다면 `API_KEY_INVALID`가 돌아왔을 것이다. 즉 이 키들은 **차단되었으나 계정에 남아 있을 수 있다.** 실질적 위험은 없지만(차단된 키는 호출되지 않는다), 콘솔에서의 실제 삭제는 별도로 확인해야 한다.

이 사실은 저장소가 공개 상태였음을 확증하기도 한다. Google이 발견했다는 것은 다른 스캐너도 발견했다고 가정해야 한다는 뜻이다.

### 이력을 다시 쓰지 않은 이유

`git filter-repo`로 지울 수 있지만 그러지 않았다. 폐기된 키는 더 이상 접근 수단이 아니라 문자열이고, 392개 커밋의 해시를 전부 바꾸는 비용에 비해 실익이 없다. 무엇보다 이력을 지우면 **이 일이 있었다는 사실도 지워진다**. 실수한 뒤 폐기했다는 기록이 남는 편이 낫다고 판단했다.

### 이 일에서 바뀐 것

- 키를 코드에 두지 않는다. 환경변수로만 주입한다.
- 쓰지 않는 SDK 의존성을 남기지 않는다 — `google-generativeai`와 `google-auth`가 import하는 코드 없이 `requirements.txt`에 남아 있었고, 그 상태는 "이 프로젝트는 Gemini를 쓴다"는 잘못된 인상을 준다.
- 공개 전 시크릿 스캔을 거친다.

## 취약점 신고

보안 문제를 발견하면 이슈로 공개하지 말고 저장소 소유자에게 직접 연락 바란다.
