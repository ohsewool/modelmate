# 보안 기록

## 과거 API 키 노출 (2026-08 조치 완료)

이 저장소의 초기 커밋 일부에 Google Gemini API 키가 평문으로 포함되어 있었다. 해당 커밋은 저장소가 공개 상태이던 기간에 존재했으므로, 그 키들은 **노출된 것으로 간주하고 폐기했다**.

| 항목 | 내용 |
|---|---|
| 영향받은 커밋 | 이력 재작성 이전의 `401f4fa`, `5b24e4a`, `75d3d90` |
| 조치 | 해당 키 전부 무효화(2026-08) |
| 검증 | 4개 키 전부 `generativelanguage.googleapis.com`에 직접 호출 → 4/4 HTTP 403 |
| 무효화 주체 | Google. 응답 메시지가 "Your API key was reported as leaked" |
| 현재 HEAD | 키 없음 |
| 현재 이력 | 키 없음 — 405개 커밋 전체에서 제거됨(2026-08) |
| 현재 코드의 Gemini 사용 | **있다.** `main_parts/040_agent_a.part`가 에이전트 코멘트에 `ask_gemini`를 사용한다 |

### 무효화가 확인된 방식, 그리고 확인되지 않은 것

키가 이력에 남아 있다는 점을 역이용해, 노출된 4개를 이력에서 추출해 실제로 API에 호출했다. 4개 전부 403이 돌아왔고 응답 메시지는 다음과 같았다.

> Your API key was reported as leaked. Please use another API key.

**따라서 이 키들은 사용 불가능하다 — 이건 계정 접근 없이 외부에서 검증된 사실이다.**

동시에 이 증거가 말해주지 *않는* 것도 분명히 해둔다. 403의 주체는 Google이다. Google의 자동 스캐너가 공개 저장소에서 키를 발견하고 차단한 것이지, 소유자가 삭제했다는 증거가 아니다. 삭제된 키였다면 `API_KEY_INVALID`가 돌아왔을 것이다. 즉 이 키들은 **차단되었으나 계정에 남아 있을 수 있다.** 실질적 위험은 없지만(차단된 키는 호출되지 않는다), 콘솔에서의 실제 삭제는 별도로 확인해야 한다.

이 사실은 저장소가 공개 상태였음을 확증하기도 한다. Google이 발견했다는 것은 다른 스캐너도 발견했다고 가정해야 한다는 뜻이다.

### 이력을 다시 쓰기로 판단을 바꾼 이유

처음에는 재작성하지 않기로 했다. 차단된 키는 접근 수단이 아니라 문자열이고, 커밋 해시를 전부 바꾸는 비용에 비해 실익이 없으며, 무엇보다 **이력을 지우면 이 일이 있었다는 사실도 지워진다**는 이유였다.

저장소를 공개하기로 하면서 그 판단을 뒤집었다. 근거가 두 가지 바뀌었다.

첫째, 비공개 저장소를 보는 사람은 없지만 **공개 저장소는 모두가 스캔한다.** 그리고 스캐너는 이 문서를 읽지 않는다. 방문자가 처음 보게 되는 것은 "실수를 기록해둔 프로젝트"가 아니라 `AIza…` 문자열 하나다. 맥락은 그 뒤에 와야 전달되는데, 순서가 반대가 된다.

둘째, 재작성에 반대한 원래 이유 — 사실이 지워진다 — 는 **이 문서가 존재함으로써 해소된다.** 지워지는 것은 키 문자열이고, 남는 것은 경위·검증·판단이다. 애초에 보존할 가치가 있던 쪽은 후자다.

포크가 0개이고 협업자가 없어 재작성 비용도 실제로는 낮았다. 조치 전 전체 백업(`git bundle --all`)을 떴고, 치환 후 239개 테스트가 그대로 통과함을 확인했다. 빈 커밋이 정리되어 408 → 405가 됐다.

기록을 남기는 것과 증거를 노출하는 것은 다르다. 전자는 이 파일이 하고, 후자는 할 이유가 없었다.

### 이 일에서 바뀐 것

- 키를 코드에 두지 않는다. 환경변수로만 주입한다.
- 공개 전 시크릿 스캔을 거친다.

### 정정 (2026-08-19)

이 문서는 한때 "현재 코드는 Gemini를 쓰지 않는다"고 적고 있었다. **틀렸다.**

근거로 삼은 검사가 `*.py`만 훑었는데, 이 백엔드는 import 시점에 `backend/main_parts/*.part`를 조립해 만들어진다. 즉 실제로 Google 라이브러리를 import하는 두 파일이 검색 범위 밖에 있었다. `001_imports_db.part`가 Google 로그인용 `id_token`을, `040_agent_a.part`가 `ask_gemini`를 네 곳에서 쓴다.

그 잘못된 결론으로 `google-generativeai`와 `google-auth`를 "안 쓰는 의존성"이라며 `requirements.txt`에서 지웠고, `MODEL_MATE_DISABLE_GEMINI`를 설정하는 줄도 "아무도 안 읽는다"며 지웠다. 셋 다 복구했다.

로컬에서는 아무것도 깨지지 않았다. 이 환경엔 두 패키지가 애초에 없어서 `backend.main`이 이미 import되지 않는 상태였기 때문이다. **다음 배포의 의존성 설치 단계에서, 원인이 된 커밋과 멀리 떨어진 곳에서 드러났을 것이다.**

그래서 규칙을 기억에 맡기지 않고 강제한다 — `tests/test_declared_dependencies.py`가 `.part`를 포함해 백엔드가 실제로 조립되는 모든 파일에서 import를 모으고, 서드파티 각각이 선언돼 있는지 확인한다. grep은 `.part`를 잊을 수 있지만 이 테스트는 못 잊는다. 첫 실행에서 미선언 의존성 두 개(`joblib`, `pydantic`)를 더 찾아냈다 — scikit-learn과 fastapi가 끌어와 우연히 동작하던 것들이다.

## 공개 전 점검 (2026-08)

전체 이력을 8종 크리덴셜 패턴으로 훑었다. `scripts/scan_secrets.py`가 그 도구다.

| 결과 | 내용 |
|---|---|
| Google API key | 4개 — `backend/main.py`, `backend/main_parts/002_auth_integrations.py`의 과거 커밋. **이후 이력에서 제거됨** |
| OpenAI key | **0개.** 최초 스캔이 175개 파일을 지목했으나 전부 오탐이었다 |
| 그 외 6종 | 0개 |

`sk-[A-Za-z0-9_-]{20,}`가 minified 번들의 `sk-image-linear-from-pos` 같은 Tailwind CSS 변수명(`--tw-mask-image-…`의 뒷부분)을 전부 잡았다. 실제 키는 접두사 뒤에 하이픈이 없어서, 패턴을 `sk-(proj-)?[A-Za-z0-9]{32,}`로 조이자 0건이 됐다.

느슨한 패턴은 안전한 쪽으로 틀리지 않는다. 175건짜리 경고는 읽히지 않고, 안 읽히는 경고 속에서 진짜 하나가 사라진다.

스캐너는 **양성 대조** 없이는 결과를 신뢰하지 않는다 — 있다고 아는 키를 못 찾으면 중단한다. 이 장치가 필요한 이유는 실제로 당했기 때문이다: 초기 버전이 `git grep -E`에 PCRE 문법 `(?:...)`을 넘겼고, git이 매번 에러로 끝났으며, 그 빈 출력이 "시크릿 없음"으로 읽혀 **키 4개가 든 저장소를 포함해 5개 전부 깨끗하다고 보고했다.**

```bash
python3 scripts/scan_secrets.py <repo>... --control <시크릿이_있다고_아는_repo>:<rev>
```

이 저장소에는 이제 알려진 시크릿이 없으므로, 대조군은 별도로 만들어야 한다 — 가짜 키 한 줄을 커밋한 임시 저장소면 충분하다. 대조 없이 나온 "clean"은 스캐너 자신이 경고하듯 검증되지 않은 결과다.

## 취약점 신고

보안 문제를 발견하면 이슈로 공개하지 말고 저장소 소유자에게 직접 연락 바란다.
